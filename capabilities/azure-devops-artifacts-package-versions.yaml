naftiko: 1.0.0-alpha2
info:
  label: Azure DevOps Artifacts API — Package Versions
  description: 'Azure DevOps Artifacts API — Package Versions. 1 operations. Lead operation: Azure DevOps Delete a package
    version. Self-contained Naftiko capability covering one Microsoft Azure Devops business surface.'
  tags:
  - Microsoft Azure Devops
  - Package Versions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_DEVOPS_API_KEY: MICROSOFT_AZURE_DEVOPS_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-devops-artifacts-package-versions
    baseUri: https://feeds.dev.azure.com/{organization}/{project}/_apis
    description: Azure DevOps Artifacts API — Package Versions business capability. Self-contained, no shared references.
    resources:
    - name: packaging-feeds-feedId-packages-packageId-versions-packageVersionId
      path: /packaging/feeds/{feedId}/packages/{packageId}/versions/{packageVersionId}
      operations:
      - name: packageversionsdelete
        method: DELETE
        description: Azure DevOps Delete a package version
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: packageId
          in: path
          type: string
          description: GUID identifier of the package
          required: true
        - name: packageVersionId
          in: path
          type: string
          description: GUID identifier of the package version
          required: true
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_DEVOPS_API_KEY}}'
  exposes:
  - type: rest
    namespace: azure-devops-artifacts-package-versions-rest
    port: 8080
    description: REST adapter for Azure DevOps Artifacts API — Package Versions. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/packaging/feeds/{feedid}/packages/{packageid}/versions/{packageversionid}
      name: packaging-feeds-feedid-packages-packageid-versions-packageversionid
      description: REST surface for packaging-feeds-feedId-packages-packageId-versions-packageVersionId.
      operations:
      - method: DELETE
        name: packageversionsdelete
        description: Azure DevOps Delete a package version
        call: azure-devops-artifacts-package-versions.packageversionsdelete
        with:
          packageId: rest.packageId
          packageVersionId: rest.packageVersionId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-devops-artifacts-package-versions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure DevOps Artifacts API — Package Versions. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-devops-delete-package-version
      description: Azure DevOps Delete a package version
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: azure-devops-artifacts-package-versions.packageversionsdelete
      with:
        packageId: tools.packageId
        packageVersionId: tools.packageVersionId
      outputParameters:
      - type: object
        mapping: $.
